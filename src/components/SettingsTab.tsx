const SettingsTab = ({ data }: { data: any[] }) => {
  // Placeholder for future admin/settings logic
  return (
    <div className="p-8 text-white/70">
      <h2 className="text-xl font-bold mb-4">Settings & Admin</h2>
      <div>Settings and admin controls will appear here.</div>
    </div>
  );
};
export default SettingsTab;
